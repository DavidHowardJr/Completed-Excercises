{
  "name": "node_fs",
  "version": "1.0.0",
  "description": "1. Open your command line and navigate to your repos directory (if you do not have a repos folder, then you can use mkdir repos to create one)\r 2. Use this template repository to start a new project in your repos folder: git clone <repo_name>\r 3. cd repo_name to navigate into your new repo directory\r 4. Start Visual Studio Code and select 'Open Folder'. Then select repo_name to open the folder in the editor (or just type code . in your terminal inside the repo directory)\r 5. Follow the instructions on the README.md file to complete exercises",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "webpack",
    "init": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.69.1",
    "webpack-cli": "^4.9.2"
  }
}
